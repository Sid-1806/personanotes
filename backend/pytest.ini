[pytest]
# Only collect the real test suite. The ad-hoc scripts test_db.py / test_models.py /
# test_pipeline.py are manual dev scripts (they need a live DB / external services),
# not pytest tests, so they are intentionally excluded from default collection.
testpaths = tests
